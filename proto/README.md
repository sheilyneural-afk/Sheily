# Protocolos Protobuf

Los paquetes siguen `noosfera.<dominio>.v1`. Los mensajes cruzan fronteras; los objetos internos pueden usar modelos propios, pero se convierten al contrato antes de publicarse.

Reglas de compatibilidad se aplican mediante Buf. Los números de campo no se reutilizan.
