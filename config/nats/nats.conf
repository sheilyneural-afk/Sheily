server_name: noosfera-node
port: 4222
http_port: 8222

jetstream {
  store_dir: /data
  max_memory_store: 1GB
  max_file_store: 20GB
}

max_payload: 8MB
write_deadline: 5s

# Producción debe activar identidades NKey por servicio y ACL generadas desde
# registry/topics.yaml. El entorno local permanece en una red Docker interna.
